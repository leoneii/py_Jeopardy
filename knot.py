# This Python file uses the following encoding: utf-8
from PySide6 import QtCore
from PySide6 import QtWidgets
from PySide6.QtWidgets import (QApplication, QLabel, QPushButton, QRadioButton,
        QSizePolicy, QWidget, QFrame)

class knoT(QLabel):
    def __init__(self):
        pass
        self.setWordWrap(True)
     # self.setText(ltxt)
      # self.temb.setFont(font)
      # self.temb.setFrameShape(QFrame.Box)
        ##        btn=Button(text = txt,font=helv36,bg='#0000cc', fg="#ffffbb",
      ##                   command=lambda c=pn: print(btn._name))
#          btn=Button(text = txt,font=helv36,bg='#0000cc', fg="#ffffbb",
#            command=lambda c=pn: btn.destroy())
#            btn.place(x=g, y=v, width=wkn, height= hkn)
#            btn.bind('<Button-1>', kdes)
